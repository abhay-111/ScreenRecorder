# ScreenRecorder
TO run application
cd screenrecorder
npm start
<br>
To use as a desktop app
npm run make
select out folder
select screenrecorder-{machineType}
select the executable file >> pin to taskbar
